<div class="container portfolio-form">
  <h1>Edit this portfolio Item</h1>
  <%= render 'form', portfilioitem: @portfilio_item %>
  <%= link_to 'Delete', @portfolio_item, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn mt-1 btn-danger'%>
</div>
